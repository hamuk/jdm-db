import React from "react";
import { Helmet } from "react-helmet";

const Register = () => (
  <div>
    <Helmet>
      <title>Register</title>
    </Helmet>
    <h1>Register</h1>
  </div>
);

export default Register;
