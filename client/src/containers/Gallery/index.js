import React from "react";
import { Helmet } from "react-helmet";

const Gallery = () => (
  <div>
    <Helmet>
      <title>Gallery</title>
    </Helmet>
    <h1>Gallery</h1>
  </div>
);

export default Gallery;
