import React from "react";
import { Helmet } from "react-helmet";

const Browse = () => (
  <div>
    <Helmet>
      <title>Browse</title>
    </Helmet>
    <h1>Browse</h1>
  </div>
);

export default Browse;
